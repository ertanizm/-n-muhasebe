<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hızlı Satış - POS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/hizlisatis.css">
    
</head>
<body>
    <%- include('../sidebar') %>

    <div class="pos-container">
        <!-- Sol Taraf - Ürünler -->
        <div class="products-section">
            <div class="search-bar">
                <input id="productSearch" type="text" class="form-control" placeholder="Ürün Ara / Barkod..." oninput="searchProducts(this.value)" onkeydown="onProductSearchKey(event)">
            </div>
            
            <!-- Hızlı Tuşlar (100 adet, 4 sütun, scroll) -->
            
            <!-- <div class="categories">
                <button class="category-button active" onclick="filterByCategory('all')">Tümü</button>
                 Kategoriler dinamik olarak eklenecek
            </div> -->

            <div class="products-group">
                <div class="products-group-header">Hızlı Stoklar</div>
                <div class="products-group-body">
                    <div class="products-grid" id="productsGrid">
                        <!-- Ürünler dinamik olarak eklenecek -->
                    </div>
                </div>
            </div>
        </div>

        <!-- Sağ Taraf - Sepet -->
        <div class="cart-section">
            <div class="customer-search">
                <input type="text" id="customerInput" placeholder="Müşteri Seç" readonly>
                <span class="search-icon" onclick="openCustomerModal()">🔍</span>
            </div>

            <div class="cart-items" id="cartItems">
                <!-- Sepet öğeleri buraya eklenecek -->
            </div>

            <div class="cart-summary">
                <div class="summary-row hidden">
                    <span>Ara Toplam:</span>
                    <span id="subtotal">0.00 ₺</span>
                </div>
                <div class="summary-row hidden">
                    <span>KDV:</span>
                    <span id="tax">0.00 ₺</span>
                </div>
                <div  class="summary-row" style="font-weight: bold; font-size: 1.2em;">
                    <span style="font-size:larger;">Toplam:</span>
                    <span style="font-size:xx-large;" id="total">0.00 ₺</span>
                </div>

                <div class="payment-buttons">
                    <a class="payment-button" style="color:white;background:linear-gradient(135deg,#e0ac00,#ffc400);display:inline-flex;align-items:center;justify-content:center;text-decoration:none" href="/gunsonu">
                        <i class="fa fa-calendar-day"></i>&nbsp;Gün Sonu
                    </a>
                    <button class="payment-button" style="color:white;background:linear-gradient(135deg,#a855f7,#7c3aed)" onclick="openSalesList()">
                        <i class="fa fa-list"></i> Satış Listesi
                    </button>
                    <button class="payment-button card-button" onclick="showPaymentModal('card')">
                        <i class="fas fa-credit-card"></i> Kart
                    </button>
                    <button class="payment-button cash-button" onclick="showPaymentModal('cash')">
                        <i class="fas fa-money-bill-wave"></i> Nakit
                    </button>
                    <button class="payment-button" style="color:white;background:linear-gradient(135deg,#228dcd,#0ea5e9)" onclick="openPartialPaymentDialog()">
                        <i class="fa fa-columns"></i> Parçalı Ödeme
                    </button>
                    <button class="payment-button" style="color:white;background:linear-gradient(135deg,#ff0000,#ff0000)" onclick="openPartialPaymentDialog()">
                        <i class="fa fa-hand-holding"></i> Bekleme Listesi
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Ödeme Modal -->
    <div class="payment-modal" id="paymentModal">
        <div class="modal-content">
            <h4 id="paymentTitle">Ödeme</h4>
            <div class="form-group">
                <label>Ödenecek Tutar</label>
                <input type="text" id="amountToPay" class="form-control" readonly>
            </div>
            <div class="d-flex align-items-center justify-content-between" style="gap:10px;margin-top:8px;margin-bottom:4px">
                <div class="flex-grow-1">
                    <small class="text-muted">Uygulanan İndirim</small>
                    <div id="discountSummary" class="fw-semibold">- 0.00 ₺</div>
                </div>
                <div>
                    <button class="btn btn-outline-primary btn-sm" onclick="openDiscountDialog()"><i class="fa fa-percent"></i> İndirim</button>
                    <button class="btn btn-outline-secondary btn-sm" onclick="clearDiscount()">Temizle</button>
                </div>
            </div>
            <div class="form-group">
                <label>Ödenen</label>
                <input type="text" id="amountPaid" class="form-control" readonly>
            </div>
            <div class="form-group">
                <label>Para Üstü</label>
                <input type="text" id="changeAmount" class="form-control" readonly>
            </div>

            <div class="numeric-keypad">
                <button class="keypad-button" onclick="addNumber('1')">1</button>
                <button class="keypad-button" onclick="addNumber('2')">2</button>
                <button class="keypad-button" onclick="addNumber('3')">3</button>
                <button class="keypad-button" onclick="addNumber('4')">4</button>
                <button class="keypad-button" onclick="addNumber('5')">5</button>
                <button class="keypad-button" onclick="addNumber('6')">6</button>
                <button class="keypad-button" onclick="addNumber('7')">7</button>
                <button class="keypad-button" onclick="addNumber('8')">8</button>
                <button class="keypad-button" onclick="addNumber('9')">9</button>
                <button class="keypad-button" onclick="addNumber('.')">.</button>
                <button class="keypad-button" onclick="addNumber('0')">0</button>
                <button class="keypad-button" onclick="clearAmount()">C</button>
            </div>

            <div class="modal-buttons" style="margin-top: 20px;">
                <button class="btn btn-secondary" onclick="closePaymentModal()">İptal</button>
                <button class="btn btn-primary" onclick="completePayment()">Ödemeyi Tamamla</button>
            </div>
        </div>
    </div>

    <!-- Müşteri Seçim Modal -->
    <div class="modal fade" id="customerModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Müşteri Seç</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <input type="text" class="form-control mb-3" placeholder="Müşteri Ara...">
                    <div class="customer-list">
                        <!-- Müşteriler buraya listelenecek -->
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="/js/script.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        let cart = [];
        let selectedCustomer = null;
        let currentPaymentMethod = null;
        let taxCodes = [];
        let globalDiscount = { type: 'none', value: 0 }; // type: none|percent|amount

        // Sayfa yüklendiğinde
        document.addEventListener('DOMContentLoaded', function() {
            renderQuickButtons();
            loadQuickButtonAssignments();
            loadTaxCodes();
            autoSelectCashCustomer();
        });

        async function autoSelectCashCustomer(){
            try {
                const res = await fetch('/api/cariler');
                const list = await res.json();
                const pesin = (list || []).find(c => (c.carikodu||'').toUpperCase() === 'PEŞİN');
                if (pesin) {
                    selectedCustomer = pesin;
                    document.getElementById('customerInput').value = pesin.unvan || pesin.carikodu;
                }
            } catch(e) { /* sessiz geç */ }
        }
        async function loadTaxCodes(){
            try{
                const res = await fetch('/api/tax-codes');
                const json = await res.json();
                if(json.success){
                    taxCodes = json.data || [];
                }
            }catch(e){ console.error('Vergi kodları alınamadı', e); }
        }

        // Products grid içine 100 adet sabit, 4 sütun düzeninde boş buton render et
        function renderQuickButtons() {
            const grid = document.getElementById('productsGrid');
            if (!grid) return;
            grid.innerHTML = '';
            const fragment = document.createDocumentFragment();
            for (let i = 0; i < 100; i += 1) {
                const btn = document.createElement('button');
                btn.type = 'button';
                btn.className = 'quick-button';
                btn.dataset.index = i;
                btn.onclick = () => onQuickButtonClick(i);
                fragment.appendChild(btn);
            }
            grid.appendChild(fragment);
        }

        // Quick button state
        const quickButtonStocks = {}; // {index: {id, stokKodu, stokAdi}}

        async function loadQuickButtonAssignments() {
            try {
                const res = await fetch('/api/quick-buttons');
                const json = await res.json();
                if (!json.success) return;
                json.data.forEach(assign => {
                    const { buttonIndex, stokId, stokKodu, stokAdi, fiyat1, fiyat2 } = assign;
                    if (stokId) {
                        quickButtonStocks[buttonIndex] = { id: stokId, stokKodu, stokAdi, fiyat1: Number(fiyat1||0), fiyat2: Number(fiyat2||0) };
                        const btn = document.querySelector(`.quick-button[data-index="${buttonIndex}"]`);
                        if (btn) btn.textContent = stokAdi || stokKodu || '';
                    }
                });
            } catch (e) {
                console.error('Hızlı tuşlar yüklenemedi', e);
            }
        }

        // Modal: stok seçimi
        let activeQuickButtonIndex = null;
        function onQuickButtonClick(index) {
            // Eğer butona atanmış stok varsa, sepete ekle
            if (quickButtonStocks[index]) {
                const s = quickButtonStocks[index];
                // Hızlı butondan eklerken fiyat1/fiyat2'yi de aktar
                addToCart({ id: s.id, stok_adi: s.stokAdi, fiyat1: s.fiyat1 || 0, fiyat2: s.fiyat2 || 0 });
                return;
            }
            // Yoksa stok seçme modalı aç
            activeQuickButtonIndex = index;
            openStockPickerModal();
        }

        function openStockPickerModal() {
            const modalEl = document.getElementById('stockPickerModal');
            const bsModal = new bootstrap.Modal(modalEl);
            bsModal.show();
            loadActiveStocksIntoPicker();
        }

        async function loadActiveStocksIntoPicker() {
            const tbody = document.querySelector('#stockPickerTable tbody');
            tbody.innerHTML = '<tr><td colspan="2">Yükleniyor...</td></tr>';
            try {
                const res = await fetch('/api/stoklar');
                const stocks = await res.json();
                tbody.innerHTML = '';
                stocks.forEach(s => {
                    const tr = document.createElement('tr');
                    tr.innerHTML = `<td>${s.stok_kodu || ''}</td><td>${s.stok_adi}</td>`;
                    tr.style.cursor = 'pointer';
                    tr.onclick = () => assignStockToQuickButton(s.id, s.stok_adi);
                    tbody.appendChild(tr);
                });
            } catch (e) {
                tbody.innerHTML = '<tr><td colspan="2">Stoklar yüklenemedi</td></tr>';
            }
        }

        async function assignStockToQuickButton(stokId, stokAdi) {
            if (activeQuickButtonIndex == null) return;
            await fetch(`/api/quick-buttons/${activeQuickButtonIndex}`, {
                method: 'PUT',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ stokkayitno: stokId })
            });
            // UI güncelle
            quickButtonStocks[activeQuickButtonIndex] = { id: stokId, stokAdi };
            const btn = document.querySelector(`.quick-button[data-index="${activeQuickButtonIndex}"]`);
            if (btn) btn.textContent = stokAdi;
            bootstrap.Modal.getInstance(document.getElementById('stockPickerModal')).hide();
            activeQuickButtonIndex = null;
        }

        // Ürünleri yükle
        function loadProducts() {
            fetch('/api/stoklar')
                .then(response => response.json())
                .then(products => {
                    const productsGrid = document.getElementById('productsGrid');
                    productsGrid.innerHTML = '';
                    
                    products.forEach(product => {
                        const card = document.createElement('div');
                        card.className = 'product-card';
                        card.onclick = () => addToCart(product);
                        card.innerHTML = `
                            <img src="/images/product-placeholder.png" alt="${product.stok_adi}">
                            <div>${product.stok_adi}</div>
                            <div>${Number(product.fiyat1||0).toFixed(2)} ₺</div>
                        `;
                        productsGrid.appendChild(card);
                    });
                })
                .catch(error => console.error('Ürünler yüklenirken hata:', error));
        }

        // Geçici: Kategoriler kullanılmıyorsa no-op fonksiyon
        function loadCategories() {}

        // Sepete ürün ekle
        function addToCart(product) {
            const existingItem = cart.find(item => item.id === product.id);
            
            if (existingItem) {
                existingItem.quantity += 1;
                existingItem.total = existingItem.quantity * (Number(existingItem.price)||0);
            } else {
                const fiyat1 = Number(product.fiyat1 ?? 0);
                const fiyat2 = Number(product.fiyat2 ?? 0);
                // Kural:
                // - Yalnız fiyat1 varsa: priceMode=fiyat1, price=fiyat1
                // - Yalnız fiyat2 varsa: priceMode=fiyat2, price=fiyat2
                // - Her ikisi de varsa: default MANUEL, price=0 (kullanıcı seçer)
                // - İkisi de yoksa: MANUEL 0
                let priceMode = 'manual';
                let priceNum = 0;
                if (fiyat1 > 0 && fiyat2 <= 0) { priceMode = 'fiyat1'; priceNum = fiyat1; }
                else if (fiyat2 > 0 && fiyat1 <= 0) { priceMode = 'fiyat2'; priceNum = fiyat2; }
                else if (fiyat1 > 0 && fiyat2 > 0) { priceMode = 'manual'; priceNum = 0; }
                cart.push({
                    id: product.id,
                    name: product.stok_adi,
                    price: priceNum,
                    fiyat1: fiyat1,
                    fiyat2: fiyat2,
                    priceMode: priceMode,
                    quantity: 1,
                    tax: 0,
                    percent: 0,
                    total: priceNum
                });
            }
            
            updateCartDisplay();
        }

        // Sepeti güncelle
        function updateCartDisplay() {
            const cartItems = document.getElementById('cartItems');
            cartItems.innerHTML = '';
            
            let subtotal = 0;
            let totalKdv = 0;
            
            cart.forEach(item => {
                const itemElement = document.createElement('div');
                itemElement.className = 'cart-item';
                // Vergi combobox'ını vergi kartından doldur
                const taxOptions = (taxCodes && taxCodes.length)
                    ? taxCodes.map(t => `<option value="${t.oran}" ${Number(item.tax||0)===Number(t.oran)?'selected':''}>${t.vergikodu}</option>`).join('')
                    : `<option value="0">KDV</option>`;

                const labelF1 =/* Number(item.fiyat1||0) > 0 ? formatPrice(item.fiyat1) : */'Satış Fiyatı 1';
                const labelF2 =/* Number(item.fiyat2||0) > 0 ? formatPrice(item.fiyat2) : */'Satış Fiyatı 2';

                itemElement.innerHTML = `
                    <div>${item.name}</div>
                    <div class="quantity-controls">
                        <button onclick="updateQuantity(${item.id}, -1)">-</button>
                        <span>${item.quantity}</span>
                        <button onclick="updateQuantity(${item.id}, 1)">+</button>
                    </div>
                    <div>
                        <select onchange="changePriceMode(${item.id}, this.value)" style="margin-right:6px">
                            <option value="manual" ${item.priceMode==='manual'?'selected':''}>Manuel</option>
                            <option value="fiyat1" ${item.priceMode==='fiyat1'?'selected':''}>${labelF1}</option>
                            <option value="fiyat2" ${item.priceMode==='fiyat2'?'selected':''}>${labelF2}</option>
                        </select>
                        <input type="number" step="0.01" value="${Number(item.price||0).toFixed(2)}" onchange="changePrice(${item.id}, this.value)" style="width:90px" placeholder="Fiyat" /> ₺
                    </div>
                    <div>
                        <select onchange="changeTax(${item.id}, this.value)">${taxOptions}</select>
                    </div>
                    <div>
                        <select onchange="changePercent(${item.id}, this.value)">
                            <option value="0" ${Number(item.percent||0)===0?'selected':''}>%0</option>
                            <option value="1" ${Number(item.percent||0)===1?'selected':''}>%1</option>
                            <option value="10" ${Number(item.percent||0)===10?'selected':''}>%10</option>
                            <option value="20" ${Number(item.percent||0)===20?'selected':''}>%20</option>
                        </select>
                        <button aria-label="Satırı Sil" onclick="removeItem(${item.id})" style="margin-left:8px;background:#dc3545;color:#fff;border:none;border-radius:4px;padding:4px 8px;cursor:pointer">×</button>
                    </div>
                `;
                cartItems.appendChild(itemElement);

                // Satır toplamını hesapla (elle girilmiş fiyat ve seçilen KDV üzerinden)
                const price = Number(item.price)||0;
                const qty = Number(item.quantity)||0;
                const base = qty * price;
                const lineWithPercent = base + (base * (Number(item.percent||0)/100));
                const discount = Number(item.discountAmount||0);
                const netBase = Math.max(0, lineWithPercent - discount);
                const lineKdv = netBase * (Number(item.tax||0)/100);
                const lineTotal = netBase + lineKdv;
                item.total = lineTotal;
                subtotal += item.total;
                totalKdv += lineKdv;
            });
            
            const tax = totalKdv; // seçilen oranlara göre toplanan KDV
            const total = subtotal; // item.total zaten KDV dahil
            
            document.getElementById('subtotal').textContent = subtotal.toFixed(2) + ' ₺';
            document.getElementById('tax').textContent = tax.toFixed(2) + ' ₺';
            document.getElementById('total').textContent = total.toFixed(2) + ' ₺';
            // Ödeme modalı açıkken indirim özetini güncelle
            const discountSum = cart.reduce((s,i)=> s + Number(i.discountAmount||0), 0);
            const ds = document.getElementById('discountSummary');
            if (ds) ds.textContent = `- ${discountSum.toFixed(2)} ₺`;
            // Ödeme modalı açıksa ödenecek tutarı da güncelle
            const atp = document.getElementById('amountToPay');
            if (atp) {
                atp.value = total.toFixed(2);
                // Para üstünü de canlı güncelle
                const paidVal = parseFloat(document.getElementById('amountPaid')?.value || '0') || 0;
                const change = paidVal - total;
                const ch = document.getElementById('changeAmount');
                if (ch) ch.value = change >= 0 ? change.toFixed(2) : '0.00';
            }
        }

        function formatPrice(val){
            const n = Number(val)||0;
            if (Number.isInteger(n)) return n.toString();
            return n.toLocaleString('tr-TR', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
        }

        // Ürün miktarını güncelle
        function updateQuantity(productId, change) {
            const item = cart.find(item => item.id === productId);
            if (item) {
                item.quantity += change;
                if (item.quantity <= 0) {
                    cart = cart.filter(i => i.id !== productId);
                } else {
                    item.total = item.quantity * item.price;
                }
                updateCartDisplay();
            }
        }

        function changePrice(productId, value){
            const item = cart.find(i=>i.id===productId);
            if(!item) return;
            item.price = parseFloat(value)||0;
            updateCartDisplay();
        }

        function changeTax(productId, value){
            const item = cart.find(i=>i.id===productId);
            if(!item) return;
            item.tax = parseFloat(value)||0;
            updateCartDisplay();
        }

        function changePercent(productId, value){
            const item = cart.find(i=>i.id===productId);
            if(!item) return;
            item.percent = parseFloat(value)||0;
            updateCartDisplay();
        }

        function removeItem(productId){
            cart = cart.filter(i => i.id !== productId);
            updateCartDisplay();
        }

        function changePriceMode(productId, mode){
            const item = cart.find(i=>i.id===productId);
            if(!item) return;
            item.priceMode = mode;
            if(mode==='fiyat1') item.price = Number(item.fiyat1||0);
            else if(mode==='fiyat2') item.price = Number(item.fiyat2||0);
            // manuel ise kullanıcı inputu belirler, mevcut price korunur
            updateCartDisplay();
        }

        // Ödeme modalını göster
        function showPaymentModal(method) {
            if (cart.length === 0) {
                Swal.fire({ icon: 'warning', title: 'Sepet boş', timer: 1500, showConfirmButton: false });
                return;
            }
            
            currentPaymentMethod = method;
            const total = calculateTotal();
            
            document.getElementById('paymentTitle').textContent = 
                method === 'cash' ? 'Nakit Ödeme' : 'Kart ile Ödeme';
            document.getElementById('amountToPay').value = total.toFixed(2);
            document.getElementById('amountPaid').value = '';
            document.getElementById('changeAmount').value = '';
            
            document.getElementById('paymentModal').style.display = 'flex';
            // indirim özeti güncelle
            const discountSum = cart.reduce((s,i)=> s + Number(i.discountAmount||0), 0);
            document.getElementById('discountSummary').textContent = `- ${discountSum.toFixed(2)} ₺`;
        }

        // Ödeme modalını kapat
        function closePaymentModal() {
            document.getElementById('paymentModal').style.display = 'none';
        }

        // Toplam tutarı hesapla (satır toplamları zaten KDV dahil)
        function calculateTotal() {
            return cart.reduce((sum, item) => sum + (Number(item.total)||0), 0);
        }

        // Ödemeyi tamamla
        function completePayment() {
            const total = calculateTotal();
            const paid = parseFloat(document.getElementById('amountPaid').value) || 0;
            // parçalı ödeme girişleri
            const pCash = window.__partialPayment ? Number(window.__partialPayment.cash||0) : 0;
            const pCard = window.__partialPayment ? Number(window.__partialPayment.card||0) : 0;
            const partsTotal = pCash + pCard;

            if (partsTotal > 0) {
                if (Math.abs(partsTotal - total) > 0.009) {
                    Swal.fire({ icon: 'error', title: 'Parçalı ödeme toplamı yanlış', text: 'Parçalı ödeme toplamı genel toplamla aynı olmalı.' });
                    return;
                }
            } else if (currentPaymentMethod === 'cash' && paid < total) {
                Swal.fire({ icon: 'error', title: 'Yetersiz ödeme', text: 'Nakit tutar toplamdan az olamaz.' });
                return;
            }

            // İrsaliye oluştur – satırlardan KDV ve ara toplamı hesapla
            let aratoplam = 0;
            let kdvtoplam = 0;
            cart.forEach(item => {
                const price = Number(item.price)||0;
                const qty = Number(item.quantity)||0;
                const base = qty * price;
                const percent = Number(item.percent||0)/100;
                const taxRate = Number(item.tax||0)/100;
                const baseWithPercent = base + (base * percent);
                const discount = Number(item.discountAmount||0);
                const netBase = Math.max(0, baseWithPercent - discount);
                const lineKdv = netBase * taxRate;
                aratoplam += netBase;
                kdvtoplam += lineKdv;
            });
            const irsaliyeData = {
                carikayitno: selectedCustomer ? selectedCustomer.id : null,
                depokayitno: 1,
                fis_tipi: 1,
                aratoplam: Number(aratoplam.toFixed(2)),
                kdvtoplam: Number(kdvtoplam.toFixed(2)),
                geneltoplam: Number((aratoplam + kdvtoplam).toFixed(2)),
                durum: 1,
                tipi: 1,
                odeme_tipi: (currentPaymentMethod === 'card') ? 'kart' : 'nakit',
                urunler: cart.map(item => ({
                    urun_adi: item.name,
                    miktar: item.quantity,
                    birim: 'Adet',
                    kdvorani: Number(item.tax||0),
                    iskontorani: Number(item.discountRate||0),
                    iskontotutar: Number(item.discountAmount||0),
                    tutar: Number(item.total||0),
                    stokkayitno: item.id
                })),
                odemeler: (partsTotal>0) ? [
                    ...(pCash>0?[{ tip:'nakit', tutar:Number(pCash.toFixed(2)) }]:[]),
                    ...(pCard>0?[{ tip:'kart', tutar:Number(pCard.toFixed(2)) }]:[])
                ] : undefined
            };

            // İrsaliyeyi kaydet
            fetch('/api/irsaliye', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(irsaliyeData)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    Swal.fire({ icon: 'success', title: 'Satış tamamlandı', timer: 1500, showConfirmButton: false });
                    cart = [];
                    updateCartDisplay();
                    closePaymentModal();
                } else {
                    Swal.fire({ icon: 'error', title: 'Hata', text: 'Satış kaydedilemedi: ' + (data.message||'') });
                }
            })
            .catch(error => {
                console.error('Satış kaydedilirken hata:', error);
                Swal.fire({ icon: 'error', title: 'Hata', text: 'Satış kaydedilirken bir hata oluştu.' });
            });
        }

        // Tuş takımı işlemleri
        function addNumber(num) {
            const input = document.getElementById('amountPaid');
            if (num === '.' && input.value.includes('.')) return;
            input.value = input.value + num;
            updateChange();
        }

        function clearAmount() {
            document.getElementById('amountPaid').value = '';
            document.getElementById('changeAmount').value = '';
        }

        function updateChange() {
            const total = calculateTotal();
            const paid = parseFloat(document.getElementById('amountPaid').value) || 0;
            const change = paid - total;
            document.getElementById('changeAmount').value = change >= 0 ? change.toFixed(2) : '0.00';
        }

        // Parçalı ödeme modalı
        function openPartialPaymentDialog(){
            const total = calculateTotal();
            Swal.fire({
                title: 'Parçalı Ödeme',
                width: 700,
                html: `
                    <div class="text-start">
                        <div class="mb-2"><b>Toplam:</b> ${total.toFixed(2)} ₺</div>
                        <div class="row g-2">
                            <div class="col-6">
                                <label class="form-label">Nakit (₺)</label>
                                <input id="ppCash" type="number" step="0.01" class="form-control" placeholder="0.00">
                            </div>
                            <div class="col-6">
                                <label class="form-label">Kart (₺)</label>
                                <input id="ppCard" type="number" step="0.01" class="form-control" placeholder="0.00">
                            </div>
                        </div>
                        <div class="d-flex align-items-center justify-content-between mt-3">
                            <div>
                                <button class="btn btn-outline-secondary btn-sm" id="ppHalfBtn">%50 / %50</button>
                                <button class="btn btn-outline-secondary btn-sm" id="ppAllCash">Hepsi Nakit</button>
                                <button class="btn btn-outline-secondary btn-sm" id="ppAllCard">Hepsi Kart</button>
                            </div>
                            <div class="text-muted" id="ppInfo"></div>
                        </div>
                    </div>
                `,
                showCancelButton: true,
                confirmButtonText: 'Kaydet',
                cancelButtonText: 'Vazgeç',
                didOpen: () => {
                    const cash = document.getElementById('ppCash');
                    const card = document.getElementById('ppCard');
                    const info = document.getElementById('ppInfo');
                    const recalc = () => {
                        const c = parseFloat(cash.value)||0; const k = parseFloat(card.value)||0;
                        const sum = c + k; const diff = total - sum;
                        info.textContent = `Kalan: ${diff.toFixed(2)} ₺`;
                    };
                    cash.addEventListener('input', recalc);
                    card.addEventListener('input', recalc);
                    document.getElementById('ppHalfBtn').addEventListener('click', () => { cash.value=(total/2).toFixed(2); card.value=(total/2).toFixed(2); recalc(); });
                    document.getElementById('ppAllCash').addEventListener('click', () => { cash.value=total.toFixed(2); card.value='0.00'; recalc(); });
                    document.getElementById('ppAllCard').addEventListener('click', () => { card.value=total.toFixed(2); cash.value='0.00'; recalc(); });
                    recalc();
                },
                preConfirm: () => {
                    const cash = parseFloat(document.getElementById('ppCash').value)||0;
                    const card = parseFloat(document.getElementById('ppCard').value)||0;
                    if (Math.abs((cash + card) - total) > 0.009) {
                        Swal.showValidationMessage('Parçalı ödeme toplamı genel toplamla aynı olmalı.');
                        return false;
                    }
                    return { cash, card };
                }
            }).then(res => {
                if (!res.isConfirmed || !res.value) return;
                // ödeme modalı kapalı olsa bile kaydı taşıyacağız; completePayment çağrısında kullanılır
                window.__partialPayment = { cash: Number(res.value.cash.toFixed(2)), card: Number(res.value.card.toFixed(2)) };
                Swal.fire({ icon:'success', title:'Parçalı ödeme kaydedildi', timer: 1200, showConfirmButton:false });
            });
        }

        // İndirim işlemleri
        function openDiscountDialog(){
            Swal.fire({
                title: 'İndirim',
                html: `
                    <div style="text-align:left">
                        <div class="form-check">
                            <input class="form-check-input" type="radio" name="discType" id="discPercent" value="percent" checked>
                            <label class="form-check-label" for="discPercent">Yüzde (%)</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input" type="radio" name="discType" id="discAmount" value="amount">
                            <label class="form-check-label" for="discAmount">Tutar (₺)</label>
                        </div>
                        <input id="discValue" type="number" step="0.01" class="swal2-input" placeholder="Değer">
                    </div>
                `,
                focusConfirm: false,
                preConfirm: () => {
                    const type = (document.getElementById('discAmount').checked ? 'amount' : 'percent');
                    const value = parseFloat(document.getElementById('discValue').value)||0;
                    return { type, value };
                }
            }).then(res=>{
                if(!res.value) return;
                setDiscount(res.value.type, res.value.value);
            });
        }

        function setDiscount(type, value){
            globalDiscount = { type, value: Math.max(0, value) };
            // önce tüm satırların indirimlerini sıfırla
            cart.forEach(it=>{ it.discountRate = 0; it.discountAmount = 0; });
            // her satırın yüzdeli ara toplamını bul
            const bases = cart.map(it=>{
                const base = Number(it.quantity||0) * Number(it.price||0);
                return base + base * (Number(it.percent||0)/100);
            });
            const subtotal = bases.reduce((s,v)=>s+v,0);
            if (subtotal <= 0) { updateCartDisplay(); return; }
            if (type === 'percent'){
                cart.forEach((it,idx)=>{
                    it.discountRate = value;
                    it.discountAmount = Number((bases[idx] * value / 100).toFixed(2));
                });
            } else if (type === 'amount'){
                let remaining = value;
                cart.forEach((it,idx)=>{
                    let part = Number((value * (bases[idx]/subtotal)).toFixed(2));
                    // son satıra kalanı bırak
                    if (idx === cart.length - 1) part = Number(remaining.toFixed(2));
                    remaining = Number((remaining - part).toFixed(2));
                    it.discountAmount = Math.min(part, bases[idx]);
                    it.discountRate = 0;
                });
            }
            updateCartDisplay();
            // ödeme modalındaki ödenecek tutarı güncelle
            const total = calculateTotal();
            const atp = document.getElementById('amountToPay');
            if (atp) atp.value = total.toFixed(2);
        }

        function clearDiscount(){
            globalDiscount = { type: 'none', value: 0 };
            cart.forEach(it=>{ it.discountRate=0; it.discountAmount=0; });
            updateCartDisplay();
        }

        // Gün Sonu ekranı
        function openDailySummary(){
            const today = new Date().toISOString().slice(0,10);
            fetch(`/api/irsaliye/list?start=${today}&end=${today}`)
                .then(r=>r.json())
                .then(j=>{
                    if(!j.success) throw new Error('Liste alınamadı');
                    const html = renderSalesTable(j.data||[]);
                    Swal.fire({
                        title: 'Gün Sonu', width: 900, html,
                        showCloseButton: true, showConfirmButton: false
                    });
                })
                .catch(()=>Swal.fire({icon:'error',title:'Hata',text:'Gün sonu alınamadı'}));
        }

        // Satış Listesi ekranı (geliştirilmiş modal ve tasarım)
        let lastSalesListRange = null;
        async function openSalesList(){
            const { value: range } = await Swal.fire({
                title:'Tarih Aralığı', html:`
                    <input id="s1" type="date" class="swal2-input">
                    <input id="s2" type="date" class="swal2-input">`,
                preConfirm:()=>({ start: (document.getElementById('s1').value||''), end:(document.getElementById('s2').value||'') })
            });
            if (!range) return;
            lastSalesListRange = range;
            openSalesListWithRange(range);
        }

        function openSalesListWithRange(range){
            const q = range?`?start=${range.start}&end=${range.end}`:'';
            fetch(`/api/irsaliye/list${q}`)
                .then(r=>r.json())
                .then(j=>{
                    if(!j.success) throw new Error();
                    const html = renderSalesTable(j.data||[]);
                    Swal.fire({ title:'Satış Listesi', width: 1200, html, showCloseButton:true, showConfirmButton:false });
                })
                .catch(()=>Swal.fire({icon:'error',title:'Hata',text:'Satış listesi alınamadı'}));
        }

        function renderSalesTable(rows){
            const tbody = rows.map(r=>`
                <tr class="sale-header-row">
                    <td style="width:42px"><button class="btn btn-light btn-sm sale-expand" onclick="toggleSaleDetails(${r.id}, this)"><i class='fa fa-plus'></i></button></td>
                    <td class="text-muted fw-semibold">${r.fis_no}</td>
                    <td>${new Date(r.tarih).toLocaleDateString('tr-TR')}</td>
                    <td>${r.cari||''}</td>
                    <td class="text-end fw-bold">${Number(r.geneltoplam||0).toFixed(2)} ₺</td>
                    <td class="text-end"><button class="btn btn-sm btn-danger" onclick="deleteSale(${r.id})">Sil</button></td>
                </tr>
                <tr class="sale-detail-row"><td colspan="6">
                    <div class="sale-collapse" id="sale-${r.id}"><div class="sale-loading">Detay yükleniyor...</div></div>
                </td></tr>
            `).join('');
            return `<div class="sale-list">
                <table class="table table-hover table-sm sale-table">
                    <thead><tr>
                        <th></th><th>Fiş No</th><th>Tarih</th><th>Cari</th>
                        <th class=\"text-end\">Tutar</th><th class=\"text-end\">İşlem</th>
                    </tr></thead>
                    <tbody>${tbody}</tbody>
                </table>
            </div>`;
        }

        function toggleSaleDetails(id, btn){
            const box = document.getElementById(`sale-${id}`);
            const open = box.classList.contains('open');
            if (!open){
                if (!box.dataset.loaded){
                    fetch(`/api/irsaliye/detaylar?id=${id}`).then(r=>r.json()).then(j=>{
                        if(!j.success) throw new Error();
                        box.innerHTML = renderDetailsTable(j.data||[]);
                        box.dataset.loaded = '1';
                    }).catch(()=>{ box.innerHTML='<div class="text-danger p-2">Detay yüklenemedi</div>'; });
                }
                box.classList.add('open');
                btn.innerHTML = "<i class='fa fa-minus'></i>";
            } else {
                box.classList.remove('open');
                btn.innerHTML = "<i class='fa fa-plus'></i>";
            }
        }

        function renderDetailsTable(items){
            const rows = items.map(d=>`<tr>
                <td>${d.urun_adi}</td>
                <td class="text-end">${Number(d.miktar||0)}</td>
                <td>${d.birim||''}</td>
                <td class="text-end">${Number(d.kdvorani||0)}%</td>
                <td class="text-end">
                    ${Number(d.tutar||0).toFixed(2)} ₺
                    ${Number(d.iskontotutar||0)>0?`<div class='text-danger small'>- ${Number(d.iskontotutar||0).toFixed(2)} ₺ ${Number(d.iskontorani||0)>0?`(%${Number(d.iskontorani)})`:''}</div>`:''}
                </td>
                <td><span class="badge ${d.satirtipi==1?'bg-primary':'bg-success'}">${d.satirtipi==1?'Kart':'Nakit'}</span></td>
            </tr>`).join('');
            return `<div class="p-2">
                <table class="table table-bordered table-sm sale-detail-table">
                    <thead><tr>
                        <th>Ürün</th><th class="text-end">Miktar</th><th>Birim</th><th class="text-end">KDV</th><th class="text-end">Tutar</th><th>Ödeme</th>
                    </tr></thead>
                    <tbody>${rows || '<tr><td colspan="6">Detay yok</td></tr>'}</tbody>
                </table>
            </div>`;
        }

        function deleteSale(id){
            Swal.fire({
                icon:'warning', title:'Satışı sil?', showCancelButton:true,
                confirmButtonText:'Evet, sil', cancelButtonText:'Vazgeç'
            }).then(res=>{
                if(!res.isConfirmed) return;
                fetch(`/api/irsaliye/${id}`, { method:'DELETE' })
                    .then(r=>r.json()).then(j=>{
                        if(!j.success) throw new Error();
                        Swal.fire({icon:'success', title:'Silindi', timer:1000, showConfirmButton:false});
                        if (lastSalesListRange){ openSalesListWithRange(lastSalesListRange); }
                    }).catch(()=>Swal.fire({icon:'error', title:'Hata', text:'Satış silinemedi'}));
            });
        }

        // Müşteri modalı
        function openCustomerModal() {
            const modal = new bootstrap.Modal(document.getElementById('customerModal'));
            loadCustomers();
            modal.show();
        }

        function loadCustomers() {
            fetch('/api/cariler')
                .then(response => response.json())
                .then(customers => {
                    const customerList = document.querySelector('.customer-list');
                    customerList.innerHTML = '';
                    
                    customers.forEach(customer => {
                        const div = document.createElement('div');
                        div.className = 'customer-item p-2 border-bottom';
                        div.style.cursor = 'pointer';
                        div.onclick = () => selectCustomer(customer);
                        div.innerHTML = `
                            <div>${customer.unvan}</div>
                            <small class="text-muted">${customer.carikodu}</small>
                        `;
                        customerList.appendChild(div);
                    });
                })
                .catch(error => console.error('Müşteriler yüklenirken hata:', error));
        }

        function selectCustomer(customer) {
            selectedCustomer = customer;
            document.getElementById('customerInput').value = customer.unvan;
            bootstrap.Modal.getInstance(document.getElementById('customerModal')).hide();
        }

        // Ürün arama
        function searchProducts(query) {
            const products = document.querySelectorAll('.product-card');
            query = query.toLowerCase();
            
            products.forEach(product => {
                const name = product.querySelector('div').textContent.toLowerCase();
                product.style.display = name.includes(query) ? '' : 'none';
            });
        }

        async function onProductSearchKey(e){
            if(e.key !== 'Enter') return;
            const val = (document.getElementById('productSearch').value||'').trim();
            if(!val) return;
            try{
                const res = await fetch(`/api/stock/by-barcode?code=${encodeURIComponent(val)}`);
                const json = await res.json();
                if(json.success){
                    const s = json.data;
                    addToCart({ id: s.id, stok_adi: s.stok_adi, fiyat1: s.fiyat1||0 });
                    document.getElementById('productSearch').value='';
                } else {
                    Swal.fire({ icon: 'warning', title: 'Barkod bulunamadı', text: json.message || '' });
                }
            }catch(err){
                console.error(err);
                Swal.fire({ icon: 'error', title: 'Hata', text: 'Barkod aramada hata' });
            }
        }
    </script>

    <!-- Stok Seçim Modalı -->
    <div class="modal fade" id="stockPickerModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-scrollable modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Stok Seç</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <table class="table table-hover" id="stockPickerTable">
                        <thead>
                            <tr>
                                <th>Stok Kodu</th>
                                <th>Stok Adı</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
